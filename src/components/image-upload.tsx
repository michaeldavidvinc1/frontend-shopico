import React, { FC } from 'react'

interface ImageUploaderProps {
  multiple: boolean;
}

const ImageUploader: FC<ImageUploaderProps> = ({multiple}) => {
  return (
    <div>
      
    </div>
  )
}

export default ImageUploader
